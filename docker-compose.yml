version: '3.8'

services:
  ariarpg:
    build:
      context: ./AriaRPG
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - aria-data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

volumes:
  aria-data:
